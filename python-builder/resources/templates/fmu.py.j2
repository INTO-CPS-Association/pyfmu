from pyfmu.fmi2slave import Fmi2Slave
from pyfmu.fmi2types import Fmi2Causality, Fmi2Variability

slave_class = "{{class_name}}"

class {{class_name}}(Fmi2Slave):

    Fmi2Slave.author = "{{author}}"
    Fmi2Slave.modelName = "{{model_name}}"
    Fmi2Slave.description = "{{description}}"

    def __init__(self):
        super().__init__()

        # Additional initialization code
        pass

    def setup_experiment(self, start_time: float):
        pass

    def enter_initialization_mode(self):
        pass

    def exit_initialization_mode(self):
        pass

    def do_step(self, current_time: float, step_size: float) -> bool:
        return True

    def reset(self):
        pass

    def terminate(self):
        pass